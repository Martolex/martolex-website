import React from "react";

import {} from "react-bootstrap";
import RightContainer from "../utils/RightContainer";

const Cart = (props) => {
  return <RightContainer title="Cart" isOpen={props.isOpen}></RightContainer>;
};

export default Cart;
